{
  "name": "twikey-api-client",
  "version": "0.0.3",
  "description": "Twikey API wrapper",
  "homepage": "https://github.com/twikey/twikey-api-node",
  "author": "Twikey <support@twikey.com> (https://twikey.com/)",
  "repository": {
    "type": "git",
    "url": "git://github.com/twikey/twikey-api-node.git"
  },
  "bugs": "https://github.com/twikey/twikey-api-node/issues",
  "engines": {
    "node": ">=18.0.0"
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "files": [
    "dist/**/*",
    "README.md"
  ],
  "keywords": [
    "direct debit",
    "credit cards",
    "subscriptions",
    "sepa direct debit",
    "api"
  ],
  "license": "MIT",
  "scripts": {
    "test": "tsx --test --test-concurrency=1 --experimental-test-isolation=none test/*.test.ts",
    "test:perfile": "tsx --test --test-concurrency=1 test/*.test.ts",
    "typecheck": "tsc -p ./tsconfig.json",
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc -p ./tsconfig.build.json",
    "smoke": "node -e \"const {TwikeyClient} = require('./dist'); new TwikeyClient({apiKey: 'smoke', apiUrl: 'https://api.example.test/creditor'}); console.log('import + construct ok on ' + process.version);\"",
    "prepack": "npm run build",
    "dist": "npm publish"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.6.0",
    "@types/node": "^22.14.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
